apiVersion: skaffold/v1beta1
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.IMAGE_NAME}}:{{.VERSION}}"
  artifacts:
  - image: gcr.io/jenkinsxio/builder-base
    context: .
    docker:
      dockerfile: Dockerfile
  - image: gcr.io/jenkinsxio/builder-slim
    context: .
    docker:
      dockerfile: Dockerfile.slim
  - image: gcr.io/jenkinsxio/builder-ruby
    context: .
    docker:
      dockerfile: Dockerfile.ruby
  - image: gcr.io/jenkinsxio/builder-swift
    context: .
    docker:
      dockerfile: Dockerfile.swift
  googleCloudBuild:
    projectId: jenkinsxio
deploy:
  kubectl:
    manifests:
