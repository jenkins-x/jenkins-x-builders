schemaVersion: '2.0.0' # Make sure to test the latest schema version
commandTests:
- name: 'terraform'
  command: 'terraform'
  args: ['--version']
  excludedError: ['.*FAIL.*']
  expectedOutput: ['.*Terraform v0.11.*']
